# 문제 작성 가이드 (authoring-guide.md)

이 문서는 `guide.md`(개발 규율·스키마·파이썬 구현)와 짝을 이루는 문서로, **"어떻게 만들
것인가"가 아니라 "어떤 문제를 만들어야 실제로 외워지는가"**를 다룬다. 새 문제를 쓰기 전에
훑고, 특히 `mcq` 오답 보기를 고를 때 이 문서의 체크리스트를 적용한다.

대상 콘텐츠 특성: SIP 표준 규격, Diameter 프로토콜, 장비명 등 **자의적 기호(코드/번호/이름)
와 의미를 연결해 외우는 항목**이 대부분. 이런 콘텐츠에 맞춘 학습 원칙을 정리한다.

## 핵심 원칙

### 1. 오답은 "혼동쌍"에서 뽑는다 (가장 중요)

전혀 상관없는 오답은 몰라도 맞힐 수 있어 학습 효과가 없다. 실무에서 실제로 헷갈리는 것들끼리
묶어야 변별력이 생긴다.

- SIP 480(Temporarily Unavailable) → 오답은 486(Busy Here), 487(Request Terminated),
  404(Not Found) 등 같은 부류의 실패 응답. 200/500처럼 동떨어진 값은 오답으로 쓰지 않는다.
- Diameter Result-Code → 같은 xxx 대역끼리 오답을 구성한다(3xxx 문제엔 3xxx 오답,
  4xxx 문제엔 4xxx 오답). 대역을 넘나드는 오답은 "구조를 몰라도" 걸러지므로 지양.
- "위 모두" / "위 모두 아님" 류 보기는 쓰지 않는다.

### 2. 무의미 암기를 구조에 앵커링한다

숫자 하나를 고립된 사실로 외우면 가장 빨리 잊힌다. 이미 있는 구조(대역, 카테고리, 역할)에
붙여서 외우게 한다.

- Diameter Result-Code: 1xxx=정보/2xxx=성공/3xxx=프로토콜 에러/4xxx=일시적 실패/
  5xxx=영구적 실패. `explain`에 값 자체뿐 아니라 **왜 이 대역인지**를 항상 적는다.
- 장비명: 이름 자체보다 **콜 플로우/네트워크에서의 역할**에 묶어서 묻는다
  (예: "가입자 인증 정보를 갖고 있는 노드는?" → HSS, 단순 "HSS의 풀네임은?"보다 우선).

### 3. 재인(recognition)보다 회상(recall)이 강하게 남는다

`ox`나 쉬운 `mcq`는 편하지만 기억 강화 효과가 약하다(desirable difficulty). 버스 UX 제약상
`short`를 남발할 순 없으니, **실무에서 결국 맨입으로 나와야 하는 고빈도 항목**만 선별적으로
`short`에 배정한다. 판단 기준은 "이 정보를 화면 없이 말하거나 로그에서 즉시 알아봐야 하는가".

`ox`는 특히 취약하다 — 은행 전체에서 정답이 한쪽(예: 항상 false)으로 쏠리면 개념을 몰라도
"무조건 X" 전략만으로 맞힐 수 있다. **같은 주제 영역의 `ox`를 만들 때는 정답이 O인 것과
X인 것이 섞이도록 의도적으로 배분한다.** 자연스럽게 반대 극성 사실이 없다면, 기존 문항의
서술을 긍정문↔부정문으로 뒤집어(같은 사실, 반대 표현) 균형을 맞춰도 된다.

### 4. 토픽을 섞는다 (이미 정해진 셔플 방식과 일치)

SIP/Diameter를 몰아서 내지 않고 세션 전체를 뒤섞는 현재 방식을 그대로 유지한다. 비슷한
범주를 붙여 비교하며 풀면 처음엔 헷갈려도 장기적으로 구분 능력이 좋아진다는 근거(인터리빙
효과)와 일치한다. 문제 작성 시 별도 조치 불필요 — 셔플은 HTML/JS가 담당.

### 5. 원자성 + 양방향 회상

- 문제 하나당 사실 하나만. 여러 개념을 한 문제에 섞으면 오답 시 무엇을 몰랐는지 피드백이
  흐려진다.
- 이름↔역할, 코드↔의미처럼 **양방향으로 각각 문제화**한다. 한 방향만 물으면 실무에서 반대
  방향으로 떠올려야 할 때 안 나온다. (예: "486의 의미는?" 과 "상대방이 통화 중일 때
  응답 코드는?" 을 각각 별도 문제로.)

## 같은 개념, 문제를 몇 개 만들 것인가

`ox`/`mcq`는 재인 기반이라 회상보다 약하다(원칙 3). 그렇다고 모든 개념을 다중 문제로
늘리면 은행이 무한정 커진다. **다른 각도로 재구성한 문제**와 **동일 문제의 단순 복제**를
구분해서, 늘릴 가치가 있는 것만 늘린다.

- **재구성(가치 있음)**: 같은 사실을 양방향(원칙 5)이나 다른 문맥으로 물어 인출 경로를
  늘리는 것. 질문 문장을 외워서 맞히는 부작용을 막아준다.
- **단순 복제(가치 없음)**: 같은 질문을 문구만 살짝 바꿔 개수만 채우는 것. 문장 패턴을
  금방 외워버려 실질 효과가 거의 없다.

늘릴지 말지는 **중요도로 차등**한다:

- 명확하고 안 헷갈리는 사실(예: BYE = 세션 종료) → 1문제로 충분.
- 자주 헷갈리거나(혼동쌍이 있거나) 실무 임팩트가 큰 것(예: 480/486/487, Result-Code
  대역 경계) → 양방향 + 다른 문맥으로 2~3개까지 투자 가치 있음.

## 문제 유형 선택 기준

| 유형 | 적합한 경우 |
|---|---|
| `mcq` | 개념 구분, 혼동쌍 변별 (코드/AVP/Result-Code 등 대부분의 기본형) |
| `ox` | 흔한 오해나 단정적 사실 확인. 너무 쉬우면 무의미하므로 남용 금지 |
| `short` | 원칙 3의 "고빈도·맨입 필요" 항목만. 숫자·약어 등 짧은 것 |
| `order` | 절차/메시지 흐름 자체가 지식인 경우 (SIP 다이얼로그, CER/CEA 등) |

`order`는 순서뿐 아니라 각 메시지의 발신/수신 방향(`from`/`to`)도 채점 대상이다. 방향도
사실이므로 순서와 마찬가지로 `ref`로 검증한다 — 특히 "누가 먼저 응답하는가"류는 추측하기
쉬우니 원문 대조 없이 넣지 않는다.

## 새 문제 작성 전 체크리스트

- [ ] 오답(`mcq`)이 실제 혼동쌍에서 나왔는가? 동떨어진 오답은 없는가?
- [ ] `explain`에 "왜"(구조/대역/맥락)가 들어갔는가, 아니면 정답만 반복하는가?
- [ ] 이 사실이 이미 다른 방향으로 문제화되어 있는가? (양방향 커버리지 확인)
- [ ] 문제 하나에 사실 하나만 담겼는가?
- [ ] 유형이 위 표의 기준에 맞는가, 그냥 습관적으로 `mcq`를 고른 건 아닌가?
- [ ] 이 개념을 다중화할 가치가 있는가(헷갈림·중요도 큼)? 아니면 이미 충분한데
      복제만 하려는 건 아닌가?
- [ ] `ox`라면, 같은 주제 영역 안에서 O/X 정답이 한쪽으로 쏠려 있지 않은가?
- [ ] `order`라면, `from`/`to` 방향도 `ref`로 검증했는가?
- [ ] `ref`(RFC 조항)로 실제 검증 가능한가? — `guide.md`의 "사실 환각 방지" 규율과 연동.
