# SIP/Diameter 퀴즈 웹페이지 — 프로젝트 가이드 (CLAUDE.md)

버스 출퇴근(아이폰)용 SIP/Diameter 반복 학습 퀴즈. 이 문서는 어느 컴퓨터의 Claude Code
세션이든 이 프로젝트 작업을 이어가기 위한 설계 결정·작업 규율 요약이다. 아래 "확정 사항"은
이미 논의로 정해진 것이므로 재논의 없이 따르고, "열린 항목"만 진행하면서 판단한다.

이 문서는 **개발(스키마·파이썬·검증기) 규율**을 다룬다. **문제 내용을 어떻게 잘 만들지**
(학습 방법론, 오답 설계 기준, 유형 선택 기준)는 [authoring-guide.md](authoring-guide.md)를
참고한다. 새 문제를 작성하기 전엔 반드시 그 문서의 체크리스트를 먼저 본다.

## 이 파일의 위치와 사용법 (읽는 Claude에게)

- 이 파일은 저장소 루트에 `CLAUDE.md`로 두어 Claude Code가 매 세션 자동 로드하게 한다.
- 프로젝트 이동은 세션 트랜스크립트가 아니라 **git으로 폴더 전체**(YAML 문제파일, 파이썬,
  이 문서)를 옮기는 방식으로 한다. Claude Code 세션 기록은 각 컴퓨터의 `~/.claude/`에
  로컬로만 남으므로 컴퓨터 간에 따라오지 않는다. 맥락은 오직 이 문서 + 저장소 파일이 나른다.
- **매 세션 시작 시 반드시** 현재 YAML 문제파일 전체를 먼저 읽어라. 새 세션은 기존 문제의
  존재를 전혀 모른다. 파일을 읽지 않고 문제를 추가하면 중복·번호 충돌이 난다.

## 세션 간 작업 규율 (이 워크플로의 한계를 막는 안전장치)

여러 컴퓨터·여러 세션으로 문제를 추가하는 방식에는 구조적 약점이 있다. 아래는 각 약점과
그것을 막기 위해 **반드시 지킬 규칙**이다. 이건 선택이 아니라 이 방식이 무너지지 않게 하는
핵심이다.

- **사실 환각 (가장 위험).** Result-Code 값, AVP/Command Code, RFC 조항 번호 같은 정밀한
  사실은 자신 있게 틀리기 쉽다. 퀴즈에 틀린 답이 들어가면 사용자가 오답을 능동적으로
  외우게 되어 없느니만 못하다. → **RFC 원문/신뢰 가능한 1차 출처에 대조해 검증한 문제만
  추가한다.** `ref`는 필수로 채우고, 웹/문서로 조항을 확인할 수 없으면 그 문제는 넣지 않고
  사용자에게 보류를 알린다. 추측으로 `ref`를 지어내지 않는다.
- **의미 중복.** ID 중복은 검증기가 잡지만, 같은 개념을 다른 문장으로 낸 중복은 문자열
  비교로 못 잡는다. → 새 문제 작성 전 기존 문제파일을 읽고 같은 개념(같은 코드/절차)을
  다루는 문항이 있는지 눈으로 확인한다. 주기적으로 사용자가 수동 정리한다는 전제.
- **명명 드리프트.** 세션마다 태그·주제·서술 스타일이 흔들려 파편화된다(`response-code`
  vs `resp-code`). 나중에 붙일 주제 필터/제외 기능이 이 파편화에 깨진다. → 아래 "허용
  값 목록"을 벗어난 `topic`/`tags`를 검증기가 **거부**하게 한다. 새 태그·주제가 필요하면
  임의로 만들지 말고 이 문서의 목록에 먼저 추가한 뒤 쓴다.
- **지시 이탈.** 시키지 않은 스키마 "개선"이나 재해석을 하지 않는다. 사용자가 요청한
  것만 수행한다. 스키마 변경이 필요해 보이면 임의로 바꾸지 말고 사용자에게 먼저 제안한다.

### 허용 값 목록 (검증기가 이 목록으로 강제)

- `topic`: `SIP`, `Diameter` (필요 시 사용자 승인 후 이 목록에 추가)
- `difficulty`: `basic`, `advanced` (2단계 — 필수 필드)
  - `basic`(기본): 응답 클래스 구조(1xx~6xx 의미), 고빈도 코드(200/180/486/404 등),
    기본 콜 플로우(등록·세션 설정·종료), 핵심 메서드 등 "가장 먼저 외워야 하는" 것.
  - `advanced`(심화): 혼동쌍 세밀 변별, 드문 예외 코드(484/483/482/485 등), 세부 규칙
    (302 캐싱 금지, 503 Retry-After 없을 때 등), 다중 트랜잭션 뉘앙스.
  - 필터가 의미 있으려면 `basic`이 충분히 쌓여야 한다. 새 주제를 열 때 `basic`부터 채운다.
- `tags`: (초기값 — 문제를 추가하며 이 목록도 함께 관리)
  `response-code`, `method`, `header`, `message-flow`, `avp`, `command-code`,
  `result-code`, `transport`, `concept`

## 목적과 전제

- 아이폰 사파리에서 버스 안에 한 손으로 푸는 반복 학습용 퀴즈.
- 학습 진도/간격 반복(Leitner, SM-2 등)은 **쓰지 않는다.** 매 세션 랜덤 출제로 반복.
- "이미 아는 문제 제외" 기능은 **추후 검토**. 지금은 구현하지 않되, 나중에 붙일 수 있게
  문제마다 불변 ID만 미리 확보해 둔다(아래 참조).
- 상태를 저장하지 않으므로 배포는 단순하다: 생성된 단일 HTML 파일을 폰으로 받아 열면 끝.
  PWA/서비스워커/localStorage 불필요.

## 개발 순서 (사용자가 정한 방식)

1. 예시 문제 몇 개를 스키마에 맞춰 작성.
2. 문제파일을 읽어 단일 HTML로 변환하는 파이썬 프로그램 작성.
3. 폰에서 열어보며 문제 형태가 적합한지 디버깅 → 이 과정에서 스키마와 파이썬을 함께 수정.
4. 스키마가 굳은 뒤에 문제를 한 번에 대량 추가.

핵심 원칙: **스키마를 소수 예시로 먼저 확정한다.** 문제가 수십 개 쌓인 뒤 필드 구조를 바꾸면
전량을 손봐야 하므로, 구조 변경은 예시가 5~10개일 때 끝내는 게 비용이 거의 없다.

## 문제파일 형식: YAML

선정 근거:
- 여러 줄 원문(SIP 메시지, Diameter 흐름)을 블록 스칼라 `|` 로 그대로 넣을 수 있다.
  JSON은 `\n` 이스케이프로 뭉개지고 주석도 못 단다.
- 타입별로 필드가 다른 이종 구조를 자연스럽게 표현한다.
- 파이썬 `PyYAML` 의존성 하나만 추가하면 된다.

### 도메인 함정 (반드시 지킬 규칙)

SIP/Diameter 자료는 콜론+공백 천지다(`Result-Code: 2001`, `Via: SIP/2.0/UDP ...`,
`CSeq: 1 INVITE`). YAML은 `키: 값` 문법이라 값 안의 `: `가 파싱 오류를 낸다.

- **한 줄 문자열은 무조건 따옴표로 감싼다.**
- **여러 줄은 `|` 블록 스칼라로 쓴다.**
- 검증기에서 이 규칙 위반을 잡을 수 있게 한다.

## 스키마 초안

```yaml
- id: sip-0001        # 불변 ID. 도메인 접두어 + 일련번호.
  type: mcq           # mcq | ox | short | order
  topic: SIP
  difficulty: basic   # basic | advanced (필수)
  tags: [response-code]
  question: "..."
  choices: ["...", "...", "...", "..."]
  answer: 2           # 0-based 인덱스
  explain: |
    ...
  ref: "RFC 3261 §21.4.x"
```

타입별 차이는 `answer`와 부가 필드만:

- `ox`   → `choices` 없음, `answer: true` 또는 `false`
- `short`→ `answer: ["정답", "허용표기1"]` (대소문자·공백 무시 비교)
- `order`→ `items`가 **정답 순서 그대로**의 `{msg, from, to}` 객체 배열. `msg`는 메시지
  이름, `from`/`to`는 참가자 이름(예: "발신자"/"착신자", 3자 이상도 가능 —
  Diameter처럼 HSS/EIR/OFCS 등 중간 노드가 끼는 경우도 actor 목록에 자연히 포함됨).
  HTML에서 `msg`만 보여주고 순서대로 탭해 배치하는데, **답안 영역 자체가 이미
  시퀀스 다이어그램**(참가자별 lifeline + 화살표)으로 실시간 렌더링된다. 배치한
  화살표를 다시 탭하면 방향이 바뀐다 — 단, 순환 대상은 "이론상 가능한 모든 조합"이
  아니라 **그 문제의 정답에 실제로 등장하는 (from, to) 조합만**이다(참가자가 늘어도
  탭 수가 폭발하지 않게). 채점은 순서+방향 둘 다 맞아야 정답. 정답 제출 전에는
  방향을 보여주지 않는다(스포일러 방지). 제출 후 reveal도 같은 다이어그램
  렌더러(`renderDiagram`)를 재사용해 정답을 보여준다.
  ```yaml
  items:
    - {msg: "INVITE", from: "발신자", to: "착신자"}
    - {msg: "100 Trying", from: "착신자", to: "발신자"}
  ```

### ID 규칙 (중요)

- `sip-0031`, `diam-0007` 처럼 **도메인 접두어 + 제로패딩 일련번호.**
- 한 번 부여한 ID는 문제 본문이 수정돼도 **유지**한다.
- 폐기한 문제의 번호는 **재사용 금지.**
- 이유: 향후 "이미 아는 문제 제외"가 저장할 대상이 곧 "제외할 ID 집합"이다. 인덱스나
  본문 해시를 키로 쓰면 문제 하나만 끼워 넣어도 전체가 어긋난다. 비용 0인 지금 고정한다.

### `ref` 필드 — 필수

답이 의심스러울 때 근거(RFC 조항)를 다시 찾는 비용이 줄고, 문제은행 자체가 RFC 인덱스
역할도 한다. 위 "사실 환각" 규율에 따라 **필수 필드**로 취급한다. 조항을 실제로 확인해
채우며, 확인 불가한 문제는 추가하지 않는다. 검증기가 `ref` 누락을 에러로 처리한다.

## 파이썬 프로그램에 반드시 넣을 것

### 1. 검증기 (변환기보다 우선)

대량 추가 시 실제로 터지는 건 오타·필드 누락·정답 인덱스 범위 초과다. 변환기가 조용히
넘어가면 폰에서 풀다가 발견하게 된다. 다음을 검사하고 **문제 ID와 함께 에러를 출력한 뒤
중단**한다:

- 타입별 필수 필드 존재 여부.
- `mcq`: `answer`가 `choices` 인덱스 범위 안인지.
- `ox`: `answer`가 불리언인지.
- `short`: `answer`가 비어있지 않은 리스트인지.
- `order`: `items`가 2개 이상인지.
- `id` 중복 여부, ID 형식(접두어-숫자).
- `ref`가 비어있지 않은지 (필수).
- `topic`이 허용 목록 안인지. 벗어나면 거부.
- `difficulty`가 허용 목록(`basic`/`advanced`) 안인지. 누락·오타 시 거부(필수).
- `tags`의 모든 값이 허용 목록 안인지. 벗어나면 거부(오타·파편화 차단).

### 2. 변환기

- YAML을 읽어 **데이터를 HTML 안에 인라인으로 박은 단일 HTML** 하나를 출력한다.
  (외부 JSON 참조 X — file:// 에서 fetch가 막히므로 반드시 인라인.)
- 출력물은 파일 하나로 자기완결적이어야 한다.

## HTML/JS 쪽 출제 로직 (셔플은 전부 런타임에서)

파이썬 생성 시점에 순서를 고정하면 "매번 랜덤"이 아니라 "재생성할 때만 랜덤"이 된다.
아래는 모두 **브라우저 JS에서** 처리한다.

- **덱 선택 시작 화면.** 앱을 열면 문제부터 내지 않고, `topic`(전체/SIP/Diameter)과
  `difficulty`(전체/기본/심화)를 골라 시작하는 화면을 먼저 보여준다. 선택한 부분집합만
  셔플 대상이 된다. 선택지는 데이터에 실제 존재하는 값에서 동적으로 만든다(문제 없는
  주제/난이도는 버튼을 안 만든다). 상태 저장이 없으므로 새로고침하면 다시 선택 화면.

- **세션 단위 비복원 추출**: 전체를 셔플해 앞에서부터 소진, 다 쓰면 다시 셔플. 같은 문제가
  최소 은행 크기만큼 간격을 두고 재등장한다. (단순 매번-랜덤은 방금 본 문제가 몇 개 뒤에
  또 나와서 답을 외우게 됨.)
- **`mcq` 보기 순서도 매 출제 셔플.** 안 하면 "정답은 3번"을 외우게 된다. 셔플 후에도
  원래 정답을 추적할 것.
- `order` 타입은 `items`를 셔플해 제시하고, 탭한 순서 + 탭으로 고른 방향(from/to)이
  둘 다 정답과 일치해야 정답 처리. 정답 확인 화면에서만 시퀀스 다이어그램으로 정답을
  보여준다(제출 전 노출 시 스포일러가 되므로 금지).
- **PASS(모르겠음) 버튼**: 어떤 타입이든 정답 제출 전에는 PASS로 채점 없이 바로 다음
  문제로 넘어갈 수 있다. 몰라서 찍은 오답이 오답으로 "학습"되는 걸 막기 위함 —
  틀린 답을 봤다는 사실 자체가 약한 기억으로 남을 수 있어서다.

## UX 제약 (버스 + 아이폰)

- 입력은 **탭 위주.** `mcq`/`ox`가 주력. `short`는 짧은 것(숫자·약어)만.
- 흔들리는 버스에서 타이핑은 오타율이 높다. `short` 비중을 낮게 유지.
- 드래그 앤 드롭은 쓰지 않는다. iOS 사파리는 터치에서 HTML5 드래그가 사실상 동작하지
  않고, 흔들리는 버스에서 정밀한 드래그는 탭보다 훨씬 실패율이 높다. `order` 타입은
  드래그 대신 "탭해서 순서대로 담기 + 담긴 항목을 다시 탭해서 방향 전환"으로 구현한다.
- **폰 UI를 과도하게 최적화하지 않는다.** 고객에게 배포하는 제품이 아니라 사용자
  본인만 쓰는 도구다. "진짜 못 쓰겠다" 수준만 피하면 되고, 약간 불편한 정도는
  감수 가능 — 터치 영역 픽셀 단위 튜닝, 다중 화면 크기 대응 같은 데 시간 쓰지 않는다.

## 배포

- **GitHub Pages로 배포한다 (확정, file:// 계획 폐기됨).** 아이폰에서 로컬 html 파일을
  Safari/Chrome으로 직접 여는 게 사실상 안 됨 — 파일 앱에서 탭하면 Quick Look으로 열려
  JS가 정상 동작 안 함. `file://` 그대로 가도 된다던 원래 계획은 실사용 중 이 문제로
  폐기했다(2026-07-26).
- 배포 방식: GitHub 저장소(퍼블릭, 무료 Pages는 public repo만 지원)에 레포 전체를
  올리고 Settings → Pages에서 활성화. 접속 URL: `https://<계정>.github.io/<repo>/quiz.html`.
  이 저장소는 git이 아니라 **github.com 웹 UI로 파일을 수동 업로드**하는 방식을 쓰기로
  했다 — `gh`/git 인증을 이 개발 환경에 설정하지 않기로 함. 즉, `python3 build.py`로
  `quiz.html`을 새로 만들 때마다 github.com에서 **수동으로 재업로드**해야 한다(자동
  아님). 이 폴더 자체는 로컬에 git 저장소로 초기화되어 있지 않다.
- 트레이드오프: 완전 오프라인은 포기 — 접속 시 인터넷(LTE 등)이 필요하다. 버스 환경에서
  데이터 연결이 안 되는 경우가 잦다면 다시 논의.
- 퍼블릭 repo이므로 URL을 아는 사람은 누구나 볼 수 있다(민감한 내용 없어 문제 없음).

## 진행 상황

- [x] `validator.py` — 타입별 필수 필드, id 형식/중복, topic/tags 허용 목록, ref 필수,
  `order` 타입 `{msg,from,to}` 구조 검사.
- [x] `build.py` — 검증 통과 후 `quiz.html` 단일 파일 생성. 셔플(세션 단위 비복원 추출,
  mcq 보기 순서, order 항목)과 PASS 버튼, 시퀀스 다이어그램(정답 입력 중에도, reveal
  에서도 동일 렌더러 재사용)까지 전부 HTML 내 JS가 런타임에 처리. 폰에서 실사용
  피드백 받아 order 타입 UI를 여러 차례 수정함(칩→탭 기반 다이어그램).
  실행: `python3 build.py` (레포 루트의 모든 `*.yaml`을 읽어 `quiz.html` 생성).
- [x] `sip.yaml` 26문제 — 메서드/메시지흐름(sip-0001~0006) + 응답 코드 클래스구조·
  혼동쌍(sip-0007~0026, RFC 3261 §21 전체 대조 검증됨). mcq 11 / ox 10(O:X 5:5 균형) /
  short 3 / order 2.
- [x] `difficulty` 필드 도입(2단계 basic/advanced, 필수). validator 강제, 기존 26문제
  태깅 완료(basic 12 / advanced 14). build.py에 **덱 선택 시작 화면** 추가 —
  topic·난이도를 골라 그 부분집합만 셔플. 선택지는 데이터에 존재하는 값에서 동적 생성
  (주제가 하나뿐이면 주제 줄 숨김). 헤더의 '덱 변경' 링크로 재선택.
- [x] `basic` 기본 문제 16개 추가(sip-0027~0042) — 예외 코드가 아니라 **기본 호 동작·
  고빈도 코드** 위주. order 2개(등록 인증 REGISTER→401→REGISTER→200 [RFC 3665 §2.1],
  기본 호 전체 설정+종료 [RFC 3261 §4 Figure 1]), mcq 6 / ox 6(O:X 3:3) / short 2.
  다룬 것: REGISTER/위치등록, ACK 3-way, BYE vs CANCEL, 100/180/183/200, 401/403/404
  기본 의미·혼동. 전량 RFC 3261/3665 원문 대조. 현재 총 42문제(basic 28 / advanced 14),
  ox 전체 O:X 8:8.

## 지금 열린 항목 (다음 작업)

1. **기본 콜 플로우 나머지 + basic 계속 보강.** sip-0027~0042로 1차 기본기(등록·기본 호·
   고빈도 코드)는 채웠다. 남은 기본 흐름: re-INVITE(세션 수정 §14), 인증 챌린지가 붙은
   INVITE 흐름(407→ACK→재INVITE), 3xx 리다이렉트 기본 동작. 여전히 예외 코드보다
   기본 동작 우선.
2. **SIP 응답 코드 나머지 + 헤더/인증/메시지흐름 추가.** "SIP에 문제 얼마나 만들 수
   있는지" 논의에서 잡은 전체 스코프(대략 60~80문제) 중 응답 코드 1차분만 끝난 상태.
   남은 카테고리: 헤더(Via/Route/Contact 등, ~10~15), 메시지흐름/다이얼로그 개념
   (인증 챌린지, re-INVITE 등, ~10~15), 인증(Digest, ~5~8), 전송(~3~5).
3. Diameter 문제 착수 전 스키마 재확인 — actor가 3개 이상(HSS/EIR/OFCS 등)인 흐름은
   `order` 다이어그램 렌더러가 구조적으로는 지원하지만 실제로 폰 화면에서 안 겹치는지
   아직 검증 안 됨. 첫 다중 actor 문제 만들 때 확인.
4. 폰에서 새로 추가된 26문제 다 훑어보며 오탈자/레이아웃 확인 (아직 안 함).